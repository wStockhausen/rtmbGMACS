% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{tform}
\alias{tform}
\title{Compute transforms on a vector of values}
\usage{
tform(fcn, val, force = FALSE)
}
\arguments{
\item{fcn}{\itemize{
\item vector of function names to apply
}}

\item{val}{\itemize{
\item vector of values to apply transform(s) to
}}
}
\value{
numeric (or adsparse) vector of transformed values
}
\description{
Function to compute transforms on a vector of values.
}
\details{
If the length of \code{fcn} is not equal to the length of \code{val}, it
is repeated until this condition is reached.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# example code
tfcns = c('symlogistic','symlogit',   'ident','log');
ifcns = c('symlogit',   'symlogistic','ident','exp');
vals = c(0.5,0.5,10,10);
rest = tform(tfcns,vals);
tform(ifcns,rest);
tform_inv(fcns,rest);
\dontshow{\}) # examplesIf}
}
